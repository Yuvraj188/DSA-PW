#include <iostream>
using namespace std;

 int main() {
//     int a;
//     a = cout <<-5;//
//     cout << "a: " << a << endl;// 2 will be printed the value of a will be 2

if(-5)// we put anything into if convert to bool and bool a=6 will give 1 always as studied earlier
cout << "true" << endl;


    return 0;
 }

    // Typically, we use 0 to indicate successful termination of the program, and a non-zero value (often 1 or another positive integer) to indicate some kind of error or abnormal termination.

// in switch we can use only integers and characters not float values